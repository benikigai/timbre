{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd packages/shared && npm install && cd ../frontend && npm install && npx tsc -b && npx vite build --outDir ../../public/app --base /app/ --emptyOutDir",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://eliass-mac-mini.tail365038.ts.net:8443/api/:path*"
    },
    { "source": "/app/(.*)", "destination": "/app/index.html" }
  ]
}
